# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.ruff_cache/
.pytest_cache/
.mypy_cache/
*.egg-info/
build/

# Node
node_modules/
web/dist/
web/.vite/
web/dist-tsbuild/
web/tsconfig.tsbuildinfo

# Local secrets / data
.env
.env.*
!.env.example
*.key
*.pem
uploads/
attachments/

# Build-time artifacts (written by deploy.sh)
# Путь именно /VERSION: каталога backend/ в репо нет, поэтому старое правило
# не срабатывало — deploy.sh пачкал отслеживаемый файл, и следующий деплой
# ложно клеймился «-dirty» в стампе версии.
/VERSION
web/public/version.json

# OS
.DS_Store
*.swp

# IDE
.idea/

# Local scratch (asana reference screenshots etc.)
.tmp-asana/
