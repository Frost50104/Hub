/**
 * Промис с потолком ожидания.
 *
 * Появился не из любви к общим утилитам: `registration.update()` в Safari на
 * десктопе не резолвится ВООБЩЕ — замер в консоли живой вкладки владельца
 * 16.09 дал 15 002 мс без ответа и без отказа. Это был единственный `await` в
 * пути кнопки «Обновить приложение» без ограничения по времени, и кнопка
 * висла навсегда: ни тоста, ни перезагрузки.
 *
 * Отказ НЕ глотаем — он проходит насквозь и остаётся ошибкой у вызывающего
 * (для обновления это обычно офлайн, и текст там свой). Ограничиваем ровно
 * одно: бесконечное молчание.
 *
 * Отдельным модулем ради теста: vitest здесь без jsdom, и импортировать ради
 * проверки хук с React и тостами нельзя.
 */

/** Метка «время вышло»: символ не спутать ни с одним значением промиса. */
export const TIMEOUT = Symbol('timeout')

export function withTimeout<T>(
  promise: Promise<T>,
  timeoutMs: number,
): Promise<T | typeof TIMEOUT> {
  return Promise.race([
    promise,
    new Promise<typeof TIMEOUT>((resolve) => {
      setTimeout(() => resolve(TIMEOUT), timeoutMs)
    }),
  ])
}
