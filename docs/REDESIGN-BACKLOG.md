# Экраны без макета — задание на дорисовку (ревизия 2026-08-21)

Первая версия этого документа (2026-08-20) перечисляла 20 экранов, которых не было в макетах
Claude Design. Дизайнер дорисовал их тем же днём — `redesign/` обновлён целиком (пять `.dc.html`).
План доведения продукта — `~/.claude/plans/claude-design-iridescent-gray.md` (шесть волн), решения
владельца: этапы доски — новая сущность, дашборд и learn-аналитика без recharts, «Управление» —
один маршрут с шестью сегментами, импорт CSV — в плане.

Ниже — текущее состояние: что закрыто, что нарисовано и ждёт реализации, чего в макетах нет
по-прежнему, и где продукт сознательно отступает от макета.

## Закрыто (волны 0–5, 2026-08-21; прод — после деплоя)

| Экран / модель | Что сделано |
|---|---|
| Фундамент | `lib/tone.ts` — единственный источник цвета статуса/приоритета; примитивы `components/ui/*`: `ListRow`, `GroupHeader`, `DropZone`, `EmptyState`/`ErrorBanner`, `Donut`, `MiniBarChart`, `MeterRow`, `PropertyRows`, `SegmentGroup`, `FilterChip`, `RightRail`, `ActionRow`, `ResponsiveDialog`, `SheetPicker`, `StatTile kpi` |
| Оболочка | `<main>`-панель на десктопе, сайдбар (роль второй строкой, нейтральный переключатель пространств, одна кнопка «Новая задача» → фокус в инлайн-поле или диалог), мобильные шапки 24px, пилюля вида `MobileViewControlBar`, круглый FAB над пилюлей (нет на дашборде и в read-only) |
| `/projects` | строки 64px, группы-папки `GroupHeader`, DnD только на десктопе с зоной приёма, перемещение через `SheetPicker`, счётчик «N проектов · M папок», пустое/ошибка раздельно |
| Календарь | десктоп: сетка gap 1px, плашки `STATUS_FILL`, просрочка сплошным красным, планка приоритета, «N просрочено», drag с подсказкой; мобильный: список дней по `due_at` |
| Хронология | sticky-колонка названий 248/150, шкала 34px, полоса «сегодня», нейтральные полосы / красные при просрочке, связи H→V→H, строки без срока (`include_undated`) и «N без срока» |
| Дашборд | без recharts: `StatTile kpi`, `Donut` ×2 (+ срез «По этапам» из `stage_breakdown`), `MiniBarChart`, таблица загрузки с колонкой «Просрочено» (`overdue_count`), `MeterRow` |
| Настройки + профиль | один экран, три вкладки (Учётная запись / Уведомления / Оформление), `/profile` → `/settings/account`, красная «Выйти» внизу мобильного |
| Карточка задачи (моб.) | `PropertyRows` (Этап, Приоритет, Старт, Срок), sticky-футер «Комментарий… / Готово» |
| Этапы доски | сущность `project_stages` (alembic 0040): колонки = этапы, «+ Этап», переименование/статус/порядок/удаление с переносом, «N из M», select этапов в карточке, `tasks.status` — зеркало |
| Импорт из CSV | `POST /projects/{id}/tasks/import?dry_run=` + `ImportTasksDialog` (Проверить → Импортировать, построчный отчёт); входы — пустое состояние списка и меню «…» шапки проекта (десктоп) |
| Новости | карточки 16/tint, закреплённая — амбер-рамка + бейдж, закрепить/открепить (`pinned_until` +30 дней), черновики под H1, `ActionRow`, счётчик «N новостей · M требует ознакомления» |
| Рейтинг | плашка ранга 28×28 (топ-3 амбер), два `SegmentGroup`, строка «это вы» с амбер-рамкой, «Мои сертификаты» |
| Биржа смен | карточки с рамкой по статусу, плашка «Сначала пройдите курс …» + «К курсу», подпись auto_confirm, «Лента / Мои смены», отклики «Подтвердить/Отклонить», правка открытой смены, «Снять смену» через диалог |
| Аттестации | «Мои / Отчёт / Кампании», полноэкранный прогон, отчёт «Прошли: N из M» + таблица, admin в аудитории тоже участник |
| Опросы | маршрут `/learn/surveys/:id`: eNPS 0–10 кнопками 44px с подсказкой, «Осталось ответов: N» → «Отправить», экран «Ответ записан»; список с чипом «анонимный»; CSV результатов |
| Ассортимент | маршрут `/learn/products/:id` (телефон: фото 200 → H1 → `<dl>` → «Что предложить вместе» → «Изучить по теме»; десктоп 300 \| 1fr), сетка с пустым фото-слотом, `FilterChip`, счётчик «N из M позиций», без «новинки для вас»; с 09.09 — поиск и ряд статусных чипов над категориями (только тем, кто ведёт контент), подсветка только что сохранённой плитки |
| Сертификат | типографский лист 1123×794 (подложка — прежний режим), полноэкранный просмотр на телефоне, печать A4 landscape = «Скачать PDF» |
| Каталог / курс / библиотека / урок / тест | `RightRail` 220 («Сводка», «О курсе», «Сводка»), урок 640+220, кольцо результата 112/30/5, «AI-помощник» в меню |
| «Управление» | один маршрут `/learn/admin?tab=` с семью сегментами (седьмой — «Прогресс сотрудников», 16.09) и гейтами по сегментам, старые URL — редиректы, аналитика без recharts |
| Конструктор | топбар действий (назад-чип, статус, «Глазами сотрудника», «Дубликат», «Аудитория», «Назначить», lifecycle), подтверждения диалогом; бэкенд duplicate/preview |
| Диалоги | `Dialog` ниже lg — шторка (все 35 диалогов); «Напомнить неознакомленным» в отчёте библиотеки |
| Ассистент | десктопная шапка (Unbounded 19 + чип охвата + «История»), подсказки в 3 колонки auto-fit, ol-сводка 16px, иконки ошибки/отказа, аватар `--av-fill`, шапка блока отчёта с iiko-чипом и действиями, композер r14 на телефоне |

## Нарисовано, но отложено (мелкая полировка)

- Урок на телефоне: двухстрочная PDF-панель, галерея 248×186, play 60/68 (текущие размеры близки).
- Библиотека: «Изучить по теме» в карточке документа (связи материал↔курс в модели нет).
- Курс: строка «Для кого» в рельсе (человекочитаемой аудитории в ответе курса нет).
- Сертификат: серверный PDF / «Отправить PDF» с телефона — сейчас печать браузером и
  полноэкранный просмотр.

## По-прежнему без макета

- `/search` — поиск с DSL-чипами (открывается по ⌘K).
- «Гусиная гонка» (`/learn/race`, ТВ `/p/race/<token>`, вкладка «Управление → Гонка», 17.09) — макет нарисован ПОСЛЕ кода артефактом Claude Design по реализованным экранам — https://claude.ai/artifact/VcXervWe23UGrcgqv3Xbi2 (8 артбордов: десктоп тёмный/светлый, телефон 390, состояния, ТВ 1920×1080, админка, диалог конкурса, пуши); экспорт `.dc.html` в `redesign/` — после ревью владельца. Замечания макетчика, закрытые в коде 17.09: тултип через портал (не режется карточкой), динамика словом на десктопе, потолки «Показать все/Свернуть» у лидеров и зачёта, зачёт первым в завершённом конкурсе, липкий отсчёт на телефоне. Открытое — масштаб трека 0..400 (см. `docs/tech-debt/open.md`).
- `/p/:token` — анонимный просмотр по публичной ссылке.
- Управление кампаниями аттестаций (создание, аудитория, импорт вопросов) — только участник нарисован.
- Редактор урока как рабочий экран (панель свойств, медиа, PDF-режим) — в макете каркас конструктора.
- Диалоги и оверлеи без маршрута: создание задачи/проекта/папки, пикер зависимостей, настройки
  колонок, «Поделиться» — собраны из общих примитивов, отдельно не нарисованы.

## Осознанные отступления от макетов (не дорисовывать, не править)

- Таб-бар 56px / подпись 11px (макет 50/10) — правило «ниже 12px ничего, исключение — 11px таб-бара».
- Вкладка «Участники» проекта остаётся шестой; фильтр «Срок» и стрелка сортировки в тулбаре остаются.
- Пагинации списка задач нет: список грузится целиком, «N из M» даётся серверными тоталами.
- Календарь/Хронология/Дашборд в макете «скоро» — в продукте работают и нарисованы по спеке.
- Ассистент: «Списания и потери», порог подтверждения, список разговоров, «В журнал» сверху.
- Сертификат: «Скачать PDF» на десктопе = печать браузером (A4 landscape); серверный PDF отложен.
- Импорт CSV — только фиксированный шаблон колонок и только с десктопа (в мобильной шапке нет меню
  действий; с телефона файлы не импортируют).

## Шаблоны проектов (0060, макеты 21.09 — ждут согласования владельца)

Холст Claude Design «Hub — шаблоны проектов» (копия артбордов — `redesign/Шаблоны проектов/`,
вне git). Референсы Mobbin: список шаблонов с метаданными — 7shifts
(https://mobbin.com/screens/485ee980-347b-4ef5-a271-71ad3091286c), диалог «Создать проект» с датой
старта — Wrike (https://mobbin.com/screens/ab7aec52-e1ca-47d3-8285-e1af2d1fa457), галерея с «пустым
проектом» — Dovetail (https://mobbin.com/screens/23bc287e-f753-4296-8c04-aa6e5027a43a).

| Артборд | Что показывает |
|---|---|
| Библиотека — десктоп / телефон / пусто | `/projects/templates`: строки `ListRow` с ключом, «N задач · вложения · автор · использован N раз», «Использовать»; тумблер модуля у hub-admin; на телефоне вход со страницы «Проекты» |
| Страница шаблона | обычная `ProjectPage` + плашка «Это шаблон» (точка отсчёта, «Создать проект по шаблону»), бейдж «ШАБЛОН», без «Поделиться», звезды, «Дашборда», галочек и красной просрочки |
| Участники шаблона | подпись «Состав будущего проекта… доступа к шаблону это не даёт» |
| Новый проект — десктоп / телефон | общий `CreateProjectDialog`: «Пустой / По шаблону», шаблон, название, дата старта (прошлое разрешено — с предупреждением о просрочке), папка, блок «Будет создано», предупреждения об уволенных и напоминаниях |
| Сохранить как шаблон | из «О проекте» → «Настройки»: имя, точка отсчёта, «Участники», «Вложения», что не попадёт |
| Отчёт и уведомление | тост-отчёт, строка «Входящих», пуш «Новый проект «X» — в проекте для вас N задач» |

## Время у старта и срока, «Напомнить» (0061–0062, макеты 24.09 — согласованы)

Холст Claude Design «Hub — время и напоминания» (https://claude.ai/artifact/45KHSB5NZ6w1Z67biNGUyR, копия — `redesign/Время и напоминания/`, вне git). Референсы Mobbin: даты + время + напоминание о сроке — Trello (https://mobbin.com/screens/f73fecb8-2b9b-484e-b337-041373054803), «Date & time / Before task» — Todoist (https://mobbin.com/screens/f72a6a00-5e3f-42b2-8f3d-9c672f2967c0), «Remind me» от срока — Superlist iOS (https://mobbin.com/screens/9cb50d2c-0635-4ce2-9077-e1b760c883e5).

| Артборд | Что показывает |
|---|---|
| Карточка — десктоп (пусто / время / только чтение / светлая) | «+ время» пунктирным чипом, «15:00 ×» рядом с датой и «Повтором», строка «Напомнить» чипами (взведено амбером, сработало/спит серым) |
| Меню «Напомнить» | «От срока» / «Время» / «Выбрать дату и время…», подсказка «придёт только во «Входящие»» |
| «Своё время» | модалка: дата + время + «Напомнить сегодня в 17:00», пометка «время московское» при другом поясе устройства |
| Карточка — телефон 390 / 320 | 4а (отдельная строка) и 4б (в одной строке); выбран 4б |
| Шторка «Напомнить» | «Мои напоминания» в трёх состояниях + варианты по 52px + подсказка |
| Списки, доска, календарь | «30 сен 15:00» в треке 104px, время перед названием в ячейке дня срока |
| Пуш, тост, «Входящие», настройки | «Напоминание» — «Сдать отчёт» — срок сегодня в 15:00; строка «Мои напоминания по задачам» |

