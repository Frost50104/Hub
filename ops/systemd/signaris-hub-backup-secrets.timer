[Unit]
Description=Signaris Hub — архив ключей ежедневно

[Timer]
# 00:20 UTC — после дампов (00:01) и снапшотов вложений (00:15), до retention
# sweep (00:30), который подрежет старые архивы этой же ночью.
OnCalendar=*-*-* 00:20:00
Persistent=true

[Install]
WantedBy=timers.target
